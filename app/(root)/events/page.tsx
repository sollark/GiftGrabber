"use client";
import { Section } from "@/ui/layout";
import { FC } from "react";

/**
 * EventPage
 * Renders the event list section.
 * @returns JSX.Element
 * @publicAPI
 */
const EventPage: FC = () => {
  return (
    <Section>
      <Section.Title>Event List</Section.Title>
    </Section>
  );
};

export default EventPage;
