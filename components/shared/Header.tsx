import { FC } from 'react'

const Header: FC = () => {
  return (
    <header
      style={{
        paddingBlock: '.5rem',
        paddingInline: '2rem',
      }}>
      <h1>GIFT GRABBER</h1>
    </header>
  )
}

export default Header
